name: flutter_copilot_claw
description: Flutter extensions enabling AI agents to inspect and interact with running Flutter apps via Flutter Copilot MCP.
version: 1.0.1
repository: https://github.com/dust365/flutter_copilot
resolution: workspace

environment:
  sdk: "^3.6.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
